import { GapInput, LineChartSettings } from '../types';

export const DEFAULT_GAPS: GapInput = { top: 12, right: 12, bottom: 12, left: 12 };

export const DEFAULT_LINE_CHART_SETTINGS: LineChartSettings = {
	enableSmoothing: false,
};
